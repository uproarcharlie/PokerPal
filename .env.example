# Database Configuration
# Get a free PostgreSQL database from https://neon.tech
# Or use local PostgreSQL: postgresql://postgres:password@localhost:5432/pokerpal
DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration
PORT=3000
NODE_ENV=development

# Google Maps API Key for address autocomplete
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Session Secret (REQUIRED for production - generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here_change_in_production

# Cloud Storage Configuration (Cloudflare R2 or any S3-compatible storage)
# Leave these empty to use local file storage (development only)
# Get free Cloudflare R2 at: https://dash.cloudflare.com/r2
USE_CLOUD_STORAGE=false
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=your_bucket_name
R2_PUBLIC_URL=https://your-bucket.your-domain.com
